- name: Trigger AppVeyor via GitHub IP
        run: |
          curl -v -X POST \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            -d "{}" \
            "https://ci.appveyor.com/api/github/webhook?id=5dsrsoe1ub09t44f"
