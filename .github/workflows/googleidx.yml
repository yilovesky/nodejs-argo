name: Secure Random Monitor V2

# 防止并发冲突：同一时间只允许一个工作流运行，避免重复启动
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false # 设置为 false，表示如果旧的没跑完，新的就排队或不启动，保护当前任务完成

on:
  schedule:
    # 设定基准：每 5 分钟运行一次
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  flexible-keep-alive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Tools
        run: |
          npm install playwright
          npx playwright install chromium --with-deps

      - name: Run Random Session
        env:
          TARGET_URL: ${{ secrets.TARGET_URL }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          TG_BOT_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          node -e "
          const { chromium } = require('playwright');
          (async () => {
            // 1. 随机延迟启动 (0-40分钟)
            const startWait = Math.floor(Math.random() * 40 * 60 * 1000);
            console.log('随机等待启动: ' + (startWait/60000).toFixed(1) + ' 分钟');
            await new Promise(r => setTimeout(r, startWait));

            // 强制启用 GPU 加速模拟和 1080p 窗口
            const browser = await chromium.launch({
              args: ['--disable-dev-shm-usage', '--window-size=1920,1080']
            });
            
            const context = await browser.newContext({
              extraHTTPHeaders: { 'Authorization': 'Bearer ' + process.env.HF_TOKEN },
              userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36',
              viewport: { width: 1920, height: 1080 }
            });
            
            const page = await context.newPage();
            const exec = require('child_process').execSync;

            try {
              console.log('正在访问目标页面 (上限 4 分钟以应对冷启动)...');
              
              await page.goto(process.env.TARGET_URL, { 
                waitUntil: 'domcontentloaded', 
                timeout: 240000 
              });

              console.log('页面基础结构已就绪，等待容器渲染内部桌面...');
              await new Promise(r => setTimeout(r, 45000));

              // 2. 随机停留时间 (8-15分钟)
              const stayTime = Math.floor(Math.random() * (15 - 8 + 1) + 8) * 60 * 1000;
              console.log('计划停留时长: ' + (stayTime/60000).toFixed(1) + ' 分钟');
              
              const startTime = Date.now();
              while (Date.now() - startTime < stayTime) {
                await page.mouse.move(Math.random() * 500, Math.random() * 500);
                await page.mouse.wheel(0, Math.floor(Math.random() * 200));
                await new Promise(r => setTimeout(r, 60000)); 
              }

              // 3. 最终截图
              console.log('生成保活报告...');
              await page.screenshot({ path: 'check.png', fullPage: false });
              
              // 时间修改为北京时间 (UTC+8)
              const caption = '🛡️ 巡检报告 [1080p]\n⏰ 时间: ' + new Date(new Date().getTime() + 8 * 60 * 60 * 1000).toLocaleString('zh-CN', {timeZone: 'UTC'}) + '\n⏱️ 停留: ' + (stayTime/60000).toFixed(1) + ' min\n✅ 状态: 刷新成功';
              exec('curl -s -F \"chat_id=' + process.env.TG_CHAT_ID + '\" -F \"photo=@check.png\" -F \"caption=' + caption + '\" https://api.telegram.org/bot' + process.env.TG_BOT_TOKEN + '/sendPhoto');

            } catch (err) {
              console.error('任务异常:', err.message);
              try {
                await page.screenshot({ path: 'error.png' });
                exec('curl -s -F \"chat_id=' + process.env.TG_CHAT_ID + '\" -F \"photo=@error.png\" -F \"caption=⚠️ 巡检异常报告\n错误详情: ' + err.message + '\" https://api.telegram.org/bot' + process.env.TG_BOT_TOKEN + '/sendPhoto');
              } catch (e) {
                exec('curl -s -d \"chat_id=' + process.env.TG_CHAT_ID + '&text=❌ 巡检彻底失败且无法截图: ' + err.message + '\" https://api.telegram.org/bot' + process.env.TG_BOT_TOKEN + '/sendMessage');
              }
            }
            
            await browser.close();
          })();
          "
